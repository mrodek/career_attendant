[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["cp alembic.ini /app/ || true", "cp -r alembic /app/ || true"]

[start]
cmd = "python test_simple.py"
